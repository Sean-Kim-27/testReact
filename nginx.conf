server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # 이 줄이 핵심이다. 없는 페이지 찾으면 무조건 index.html로 보내라.
        try_files $uri $uri/ /index.html;
    }
}